{
  "name": "chapter_3",
  "version": "1.0.0",
  "description": "a full stack todo application that uses a NodeJS backend, a SQLite database ans JWT Authentication ",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
